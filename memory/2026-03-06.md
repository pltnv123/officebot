# 2026-03-06

- User requires strict periodic status updates in Russian format:
  1) Сделано (only verifiable facts)
  2) В работе
  3) Следующий шаг
  4) ETA (base + buffer + total + UTC deadline)
  and explicit early warning when schedule risk exists.

- In autonomous reminder loops, user expects agent to EXECUTE the cycle (not forward reminder text):
  healthcheck -> pick one backlog item -> real code/file changes -> commit/push -> wait for green CI -> report factual result.

- Project context this period focused on `office/scripts/ops/office_healthcheck.sh` and CI reliability helpers.
  Repeated incremental improvements were made to healthcheck observability (API fallback, diagnostics, timing fields, host/version fields, configurable curl timeout/retries/connect-timeout) and to CI wait handling.

- Current operational reality observed repeatedly:
  - Build artifact freshness checks pass reliably.
  - API endpoint(s) for task counting are intermittently unreachable from healthcheck context (`/api/state`, `/api/ops/health`), so diagnostics were expanded.

- CI status trend by end of period: mostly stable green runs after each small ops/docs/script change, with occasional cancelled runs earlier due to concurrency/trigger behavior.

- Visual track (Phase 1) now includes:
  - FBX robot fallback implemented (commit f103875) so `RuntimeSceneBuilder.BuildRobot` tries `Resources/Models/<ROLE>` before primitives; keeps scene ready when FBX assets appear.
  - Warm fill point light `WarmFill` added (commit 1f7c187) to satisfy VIZ-002 brightness goal.
  - Robot labels gained backplates (commit 999d6b1) to match Pixar-style readability.
- CI discipline note: multiple failures (CS0201/CS1597) were auto-fixed immediately with `FIX:` commits (88c2b97, 1acfe08); remember null-coalescing lines can’t stand alone, helper methods must be proper blocks.
- Latest green deploy timestamp: 2026-03-06 13:52:35 UTC (WebGL.wasm.gz) corresponding to f103875.

- New AGENTS.md rule: after every green CI build, capture http://5.45.115.12/office/ screenshot and send to Telegram immediately.
