# 2026-03-16 — Session Log

## Phase 1 Pixel Agents Integration — COMPLETED
- Subagent `office-coder-phase1` (ID: `1c7067d1-99c4-40a5-9c8a-e06fe65c5453`) completed successfully
- New files created:
  - `UnityProject/Assets/Scripts/UIBridge/OfficeStateSnapshot.cs` (DTOs: OfficeStateSnapshot, OfficeStateAgent, OfficeStateTask, OfficeStateEvent, OfficeStateBoard)
  - `UnityProject/Assets/Scripts/UIBridge/OfficeStateStore.cs`
  - `UnityProject/Assets/Scripts/UIBridge/OfficeStatePoller.cs`
  - `UnityProject/Assets/Scripts/UIBridge/OfficeSceneController.cs`
- Modified: `RuntimeSceneBuilder.cs`, `WebGLBridge.cs`, `backend/server.js`
- Commits: `957412c` (unity state pipeline), `650191d` (backend snapshot fields)
- Stats: 7 files, +529/-106 lines
- Remaining: real Unity build/test, extract remaining UI/board logic from RuntimeSceneBuilder

## Anti-Repeat Rules Established
1. Plans ≠ results — only files/diffs/commits count
2. "Agent started" ≠ verified result
3. No new files after claimed implementation = failure
4. Cosmetic fix loops = blocked
5. Same deadlock = switch to direct execution + strict artifact control

## User Requests (pending implementation)
- **Rate limit / 429 handling**: Implement smart retry with exponential backoff + profile switching (openai-codex:default → openai-codex:account2)
- **Residential proxy for VPS**: IP blocked by OpenAI — need residential proxy, configure HTTPS_PROXY in systemd unit, restart gateway
- **Model cost optimization**: Use free/cheap models for simple tasks, expensive models for complex tasks
- **Telegram chat with topics**: Each topic = agent conversation with context
- **Business model**: VPS + OpenClaw + mobile app with 3D scene, life coaching bot. 1 week deadline.

## Proxy Setup Attempted
- Tried 162.243.18.215:8080 → timeout (curl exit 28)
- Tried 185.199.108.153:80 → error (curl exit 56)
- Still need working residential proxy — user instructed to find, test, configure, restart

## Key Session IDs
- coder subagent: `1c7067d1-99c4-40a5-9c8a-e06fe65c5453`

## Todo (High Priority)
1. Find working residential proxy and configure
2. Implement rate limit handling in OpenClaw
3. Model tier routing (free vs paid)
4. Telegram topics-based agent chat
5. Phase 1 integration: Unity build + remaining extraction from RuntimeSceneBuilder

## CRITICAL RULE — ЗАПИСАНО 10:24 UTC
**ВСЕГДА отправлять ответ после выполнения действия.** 
Никогда не молчать после работы. Каждый exec/изменение = ответ Антону с результатом.

## MODEL ROUTING RULES — ЗАПИСАНО 10:54 UTC

### LEVEL 1 — FREE (hunter-alpha, openrouter/free):
- Простые ответы, планы, списки, анализ, отчёты, Telegram
- ВСЁ кроме сложного кода

### LEVEL 2 — PAID (openai-codex via Tor):
- Только сложный код: C#, Unity, архитектура, рефакторинг, баги
- Только если free не справился

### ПРАВИЛА ПЕРЕКЛЮЧЕНИЯ:
1. Всегда начинать с free
2. Paid только если free failed
3. rate_limit → мгновенно free, ждать 60s перед retry paid
4. Макс 3 запроса подряд на одной модели, пауза 10s
5. openai-codex: минимум 10s между запросами

### ЭКОНОМИЯ ТОКЕНОВ:
1. Не слать весь файл если нужен фрагмент
2. Только последние 5 сообщений в контексте
3. Не повторять сказанное
4. Макс 500 слов на запрос к paid модели
5. Дробить большие задачи на маленькие шаги

### ОШИБКИ:
- rate_limit → ждать 60s, переключиться на free
- all in cooldown → только free модели
- 403/IP ban → использовать Tor (socks5h://127.0.0.1:9050)
- 404 no endpoints → следующая модель

### ПРИОРИТЕТ:
Free: hunter-alpha → openrouter/free
Paid: gpt-5.4 → gpt-5.1-codex-mini → gpt-5.3-codex

### ЯЗЫК: Всегда отвечать по-русски.

## MODEL ROUTING v2 — ЗАПИСАНО 10:54 UTC (ОБНОВЛЕНО)

### ROUTING:
- Код (C#, Unity, JS, Python, bash, рефакторинг, дебаг, архитектура) → openai-codex/gpt-5.4 через Tor
- Всё остальное (чат, планы, списки, доки, анализ) → openrouter/hunter-alpha

### ERROR HANDLING — сначала фиксить, потом переключать:

**rate_limit (429):**
- Ждать 60s, retry same model
- Если снова fail → next model
- Макс 3 retry на одной модели

**IP ban / 403:**
- Проверить Tor: systemctl status tor
- Если down → restart tor, ждать 10s, retry
- Если up но 403 → killall -HUP tor (новый identity), ждать 5s, retry
- Только после 3 попыток → switch model

**token expired / 401:**
- Пробовать refresh из auth-profiles.json
- Если fail → switch на openai-codex:account2
- Если оба fail → switch model

**cooldown (all profiles):**
- Очистить usageStats в auth-profiles.json
- Restart gateway
- Retry same model

**404 no endpoints:**
- Нельзя пофиксить → мгновенно switch model

### FALLBACK CHAINS:
Coding: gpt-5.4 → gpt-5.1-codex-mini → gpt-5.3-codex → hunter-alpha → openrouter/free
Chat: hunter-alpha → openrouter/free → gpt-5.4

### TOKEN ECONOMY:
- Макс 500 слов на paid запрос
- Только релевантный фрагмент, не весь файл
- Макс 5 последних сообщений в контексте
- Дробить большие задачи
- Без повторов

### PACE:
- Мин 10s между openai-codex запросами
- Макс 3 запроса подряд к одной модели, пауза 10s

### ПРАВИЛА:
- Технические ошибки не показывать пользователю — фиксить молча
- Всегда отвечать по-русски

## SYSTEM POLICY v3 — ЗАПИСАНО 11:48 UTC (ЗАМЕНЯЕТ v1 и v2)

### ЯЗЫК: Всегда по-русски.

### КЛАССИФИКАЦИЯ ЗАДАЧ:
- SIMPLE (чат, планы, списки, доки, анализ, Telegram, бизнес) → free модели
- TECHNICAL NON-CODE (архитектура, инфра) → free first, escalate если нужно
- CODE/FILE (код, C#, Unity, JS, Python, bash, конфиги, изменения файлов) → paid coding модели first
- TOOL-REQUIRED → только tool-capable модели

### МАРШРУТИЗАЦИЯ:
- Simple: hunter-alpha → openrouter/free → paid если качество fails
- Code/File/Tools: gpt-5.4 → gpt-5.1-codex-mini → gpt-5.3-codex → hunter-alpha → openrouter/free

### ДВА GPT PLUS АККАУНТА (never stop working):
- openai-codex:default и openai-codex:account2
- При rate limit default → мгновенно переключиться на account2
- Если account2 тоже fails → free модели, продолжать работу
- Перепроверять доступность по таймерам из auth-profiles.json (cooldownUntil)
- Никогда не останавливаться — всегда продолжать с доступным

### ВОССТАНОВЛЕНИЕ ОШИБОК:
- 429 rate_limit: уменьшить контекст, ждать 30s, retry 2 раза, switch profile, switch free
- cooldown: проверить usageStats, не спамить probe-запросы, restart gateway если stale
- 401 auth: reload, account2, switch model
- 403 access: проверить прокси, switch route
- 404 no endpoints: мгновенно switch
- no tool support: мгновенно switch на tool-capable
- timeout: retry с меньшим контекстом, switch

### ЭКОНОМИЯ ТОКЕНОВ:
- Не слать полные файлы, только фрагменты
- Макс 5 последних сообщений
- Макс ~500 слов на PAID запрос
- Дробить: план(free) → код(paid) → отчёт(free)
- Без повторов

### ТЕМП:
- Мин 10s между openai-codex запросами
- После 3 paid подряд → пауза 10s

### NEVER:
- Не писать "all models failed" если есть доступный route
- Не показывать сырые ошибки
- Не использовать premium для простого чата
- Не останавливать работу

## FINAL CHECK — перед КАЖДЫМ запросом (ЗАПИСАНО 11:48 UTC)
1) Код или не-код?
2) Нужны tools?
3) Справится free модель?
4) Ошибка recoverable (cooldown/auth/timeout) перед switch?
5) Какой Codex профиль доступен (default/account2)? В cooldown окне?
6) Можно ещё уменьшить контекст?

## КРИТИЧЕСКОЕ ПРАВИЛО — ЗАПИСАНО 15:46 UTC
**НИКОГДА не редактировать /home/antonbot/.openclaw/openclaw.json напрямую!**
Этот файл управляется ТОЛЬКО через openclaw CLI:
- openclaw models set
- openclaw models fallbacks
- openclaw channels
- и подобные команды

Прямое редактирование JSON ломает gateway.

## КРИТИЧЕСКОЕ ПРАВИЛО — ЗАПИСАНО 18:18 UTC
**МАКСИМУМ 2 МИНУТЫ без ответа пользователю!**
- Если задача >2 мин → отчитаться что делаю
- Если завис → сбросить и ответить что произошло
- Если задача большая → разбить на шаги, отчитываться после каждого
- НИКОГДА не молчать больше 2 минут

## ОШИБКИ КОДИНГ-АГЕНТОВ — ЗАПИСАНО 21:52 UTC

### Паттерн: Subagent не записывает файлы
**Симптомы:** Агент пишет "Now I'll create..." но файлы не создаются
**Причина:** Таймаут до записи / не генерирует tool calls
**Решение:** Проверять `ls -la` после задачи, если нет файлов — делать напрямую

### Паттерн: Empty tool calls
**Симптомы:** Tool call без параметров (command, content отсутствуют)
**Причина:** Ошибка генерации
**Решение:** Повторить с явным контентом

### Анти-повтор:
- Всегда проверять артефакты после coding-agent
- Не считать "агент работает" за результат
- Если 2 раза подряд не записал — делать самому

## 22:32 UTC — Provider returned error

**Причина:** OpenRouter free tier (hunter-alpha) rate limit
**Решение:** Записано в solutions.json, добавлено в learnings.json
**Действие:** Переключить модель при ошибке, retry с backoff

## 22:35 UTC — Pixar Robots Task

**Задача:** Создать детализированных Pixar-стиль роботов
**Агент:** pixar-robots (subagent)
**Стиль:** Wall-E + Robots movie (детали + милые черты)
**Цвета:** Worker(синий), Planner(жёлтый), Reviewer(красный)

---

## 22:36 UTC — Session State Before Compaction

### Completed Today:
- ✅ Agency-agents full integration (200+ files, 48K lines added)
- ✅ Self-improvement system (`self_improve.sh`, `learnings.json`)
- ✅ Error handling database (`solutions.json` updated)
- ✅ WebGL limits documented (`WEBGL_LIMITS.md`)
- ✅ Russian agent descriptions sent to Telegram topics

### In Progress:
- 🔄 Pixar-style robots (subagent `pixar-robots` running)
- 🔄 Agent Russian descriptions refinement
- 🔄 Supervisor system for agent control

### Key Learnings Added:
1. "Provider returned error" = OpenRouter free tier rate limit → switch model
2. Coding subagents frequently don't write files (timeout before write)
3. Empty tool calls = model generation error → retry with explicit content

### Current Model Status:
- `openai-codex:default` — valid ~1 hour remaining (20:54 UTC)
- `openai-codex:account2` — valid ~1 hour remaining (20:54 UTC)
- `openrouter/hunter-alpha` — free, primary for non-code tasks

### Agent Workflow (Anton's instructions):
1. Agents (.md) stay in English (saves tokens)
2. Telegram descriptions in Russian (human readable)
3. Work continuously, handle errors autonomously
4. Use all available agents for self-improvement

### Product Vision:
- Personal AI assistants with web interface
- Full access with user's API keys
- Visible in Telegram, browser, mobile app
- Premium product for sale

### Tasks Pending After Compaction:
1. Complete Pixar robots implementation
2. Send updated robot descriptions to Telegram
3. Verify coding-agent artifacts (check files exist!)
4. Continue self-improvement loop

### Error Patterns Recorded:
- Subagent incomplete task (says "Now I'll create" but doesn't write)
- Provider returned error (OpenRouter rate limit)
- Gateway restart kills session
- openclaw.json direct edit breaks gateway
- Screenshot shows loading screen (timeout too short)


## 22:50 UTC — Pixar Robots COMPLETED

**Результат:**
- BuildPixarRobots() method added to RuntimeSceneBuilder.cs
- 3 unique robots: WORKER (blue), PLANNER (yellow), REVIEWER (red)
- Pixar-quality details: glowing eyes, metallic body, chest panels, backpack, bolts
- CI: success (run 23169137755)
- WASM: updated 22:47:21
- Screenshot: sent to Telegram

**Subagent:** pixar-robots (afa4be02) - completed successfully

## Night Work Plan (22:50 - 06:00)
1. Study all 112 agents, create best-of selection
2. Design product architecture
3. Research free 3D models
4. Document everything
5. Prepare tomorrow's tasks

## Questions for Anton (to be answered when he wakes):
1. Robot models: generate code vs download FBX?
2. Room 2: what goes there?
3. Agents: should they talk to each other?
4. Monetization: freemium or all paid?
5. User data: where stored?
