# GitHub approved-write guard

Use explicit approvals, sanitized auth checks, base reset, remote branch collision checks, `git status --porcelain=v1 -uall`, strict diff allowlists, PR diff verification, check verification, and hfinalize. Never force push or push directly to main/master.
