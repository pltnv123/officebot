# QMD knowledge layer

Use safe commands only: `qmd update`, `qmd search`, `qmd get`, `qmd ls`. Important `/output` reports should have concise index notes in `/workspace/reports` or `/workspace/knowledge`. Never run vector/LLM QMD commands without approval.
