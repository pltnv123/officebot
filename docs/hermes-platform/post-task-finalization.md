# Post-task finalization

After every non-trivial task, run `qmd update` when useful and `hfinalize` before final answer. Report hfinalize status, qmd status, finalizer report path, snapshot request, and last-auto-snapshot status. Verify host snapshots via artifacts visible in `/workspace/runtime` and `/output/finalizer`.
