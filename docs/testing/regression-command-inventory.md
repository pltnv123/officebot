# Regression Command Inventory

- `npm test`: not configured
- `cd backend && npm start`: starts server, proposal only
- `node scripts/verify_ws.js`: requires backend listener
- GitHub Actions deploy: requires secrets/deploy host
- `qmd update`: safe platform health
- `hfinalize`: required platform finalizer
